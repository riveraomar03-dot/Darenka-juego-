<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Juego para Darenka 💖</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <style>
    body {
      margin: 0;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      font-family: Arial, Helvetica, sans-serif;
      background: url("https://upload.wikimedia.org/wikipedia/commons/c/c9/Ppg_hearts_bg.jpg")
        center / cover no-repeat fixed;
    }

    .card {
      background: rgba(255, 255, 255, 0.9);
      padding: 30px;
      border-radius: 20px;
      width: min(700px, 90%);
      text-align: center;
      box-shadow: 0 10px 30px rgba(0,0,0,0.25);
      position: relative;
      overflow: hidden;
    }

    h1 {
      margin-top: 0;
      font-size: 32px;
    }

    p {
      font-size: 18px;
    }

    .buttons {
      margin-top: 20px;
      display: flex;
      gap: 15px;
      justify-content: center;
      flex-wrap: wrap;
    }

    button {
      border: none;
      border-radius: 12px;
      padding: 14px 20px;
      font-size: 16px;
      font-weight: bold;
      cursor: pointer;
      transition: transform 0.1s ease;
      box-shadow: 0 6px 15px rgba(0,0,0,0.2);
    }

    button:active {
      transform: scale(0.97);
    }

    .primary {
      background: #000;
      color: #fff;
    }

    .secondary {
      background: #fff;
      color: #000;
    }

    .heart {
      position: absolute;
      bottom: -20px;
      font-size: 24px;
      animation: floatUp 3s ease forwards;
      pointer-events: none;
    }

    @keyframes floatUp {
      0% {
        transform: translateY(0) scale(0.8);
        opacity: 0;
      }
      20% {
        opacity: 1;
      }
      100% {
        transform: translateY(-500px) scale(1.6);
        opacity: 0;
      }
    }

    .final {
      font-size: 38px;
      margin-top: 10px;
    }

    .hint {
      font-size: 14px;
      opacity: 0.7;
      margin-top: 10px;
    }
  </style>
</head>

<body>

  <div class="card" id="card">
    <h1 id="title">Hola Darenka 😊</h1>
    <p id="text">Antes de la sorpresa… ¿te gustaría jugar un poquito?</p>

    <div class="buttons" id="buttons">
      <button class="primary" id="musicBtn">Activar música 💗</button>
      <button class="secondary" id="nextBtn">Sí, vamos</button>
    </div>

    <audio id="bgMusic" src="music.mp3" loop></audio>
    <audio id="popSound" src="pop.mp3"></audio>
  </div>

  <script>
    const title = document.getElementById("title");
    const text = document.getElementById("text");
    const buttons = document.getElementById("buttons");
    const card = document.getElementById("card");

    const bgMusic = document.getElementById("bgMusic");
    const popSound = document.getElementById("popSound");

    let step = -1;

    const screens = [
      {
        title: "Nivel 1 💕",
        text: "Elige una opción para continuar:",
        buttons: ["Opción A", "Opción B"]
      },
      {
        title: "Nivel 2 ✨",
        text: "¿Qué te gusta más?",
        buttons: ["Café ☕", "Helado 🍦", "Cine 🎬"]
      },
      {
        title: "Nivel 3 😳",
        text: "Última pregunta… ¿te gustan las sorpresas?",
        buttons: ["Sí 💖", "Obvio 😌"]
      },
      {
        title: "Espera…",
        text: "Respira hondo antes de ver esto 💕",
        buttons: ["Ver sorpresa 💘"]
      },
      {
        title: "💖",
        text: "Darenka, ¿quieres ser mi novia?",
        final: true,
        buttons: ["Reiniciar 🔁"]
      }
    ];

    function playPop() {
      try {
        popSound.currentTime = 0;
        popSound.play();
      } catch {}
    }

    function spawnHearts() {
      for (let i = 0; i < 8; i++) {
        const heart = document.createElement("div");
        heart.className = "heart";
        heart.innerText = ["💖","💗","💘","💝","💞"][Math.floor(Math.random()*5)];
        heart.style.left = Math.random() * 90 + "%";
        heart.style.fontSize = (20 + Math.random()*20) + "px";
        card.appendChild(heart);
        setTimeout(() => heart.remove(), 3000);
      }
    }

    function renderScreen(index) {
      step = index;
      const s = screens[index];

      title.innerText = s.title;

      if (s.final) {
        text.innerHTML = `<div class="final">${s.text}</div>
                          <div class="hint">💌 Puedes tomar captura aquí</div>`;
        spawnHearts();
      } else {
        text.innerText = s.text;
      }

      buttons.innerHTML = "";

      const musicToggle = document.createElement("button");
      musicToggle.className = "secondary";
      musicToggle.innerText = "🔊 Música on / off";
      musicToggle.onclick = () => {
        playPop();
        if (bgMusic.paused) bgMusic.play();
        else bgMusic.pause();
      };
      buttons.appendChild(musicToggle);

      s.buttons.forEach(() => {
        const btn = document.createElement("button");
        btn.className = "primary";
        btn.innerText = "Continuar 💕";
        btn.onclick = () => {
          playPop();
          spawnHearts();
          if (step + 1 < screens.length) {
            renderScreen(step + 1);
          } else {
            renderScreen(0);
          }
        };
        buttons.appendChild(btn);
      });
    }

    document.getElementById("musicBtn").onclick = () => {
      playPop();
      bgMusic.play();
      spawnHearts();
    };

    document.getElementById("nextBtn").onclick = () => {
      playPop();
      spawnHearts();
      renderScreen(0);
    };
  </script>

</body>
</html>
